"use client";

// This page allows TanStack Router to handle the root route
// TanStack Router (rendered in _app.tsx) will handle all routing logic
export default function IndexPage() {
  // Return null - TanStack Router in _app.tsx will handle the routing
  return null;
}
